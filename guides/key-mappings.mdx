---
title: "Key Mappings"
description: "A complete reference for configuring key mappings in coc.nvim, covering completion, navigation, refactoring, text objects, and CocList commands."
---

coc.nvim does not set any key mappings by default to avoid conflicts with your existing configuration. You must add mappings yourself. The examples below are drawn directly from the official example configs.

<Warning>
  Some Vim plugins modify the same keys. Before adding a mapping, verify it is
  not already taken with `:verbose imap <tab>` (for insert-mode mappings) or
  `:verbose nmap <key>` (for normal-mode mappings).
</Warning>

<Note>
  The Vimscript and Lua configs are equivalent. Use whichever matches your
  `init.vim` or `init.lua` setup. The Lua config requires Neovim 0.7.0 or
  later.
</Note>

## Recommended options

Set these options before your mappings. They improve the coc.nvim experience.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " May need for Vim (not Neovim) since coc.nvim calculates byte offset by
    " counting utf-8 byte sequences
    set encoding=utf-8

    " Some servers have issues with backup files, see #649
    set nobackup
    set nowritebackup

    " Having longer updatetime (default is 4000 ms = 4s) leads to noticeable
    " delays and poor user experience
    set updatetime=300

    " Always show the signcolumn, otherwise it would shift the text each time
    " diagnostics appear/become resolved
    set signcolumn=yes
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Some servers have issues with backup files, see #649
    vim.opt.backup = false
    vim.opt.writebackup = false

    -- Having longer updatetime (default is 4000 ms = 4s) leads to noticeable
    -- delays and poor user experience
    vim.opt.updatetime = 300

    -- Always show the signcolumn, otherwise it would shift the text each time
    -- diagnostics appeared/became resolved
    vim.opt.signcolumn = 'yes'
    ```
  </Tab>
</Tabs>

---

## Completion

Tab navigates the popup menu or triggers completion. `<CR>` confirms the selected item.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Use Tab to trigger completion with characters ahead and navigate the menu.
    " NOTE: There's always a completion item selected by default. You may want to
    " enable no-select by setting "suggest.noselect": true in your config file.
    " NOTE: Use ':verbose imap <tab>' to make sure Tab is not mapped by another
    " plugin before adding this to your config.
    inoremap <silent><expr> <TAB>
          \ coc#pum#visible() ? coc#pum#next(1) :
          \ CheckBackspace() ? "\<Tab>" :
          \ coc#refresh()
    inoremap <expr><S-TAB> coc#pum#visible() ? coc#pum#prev(1) : "\<C-h>"

    " Make <CR> accept the selected completion item or notify coc.nvim to format.
    " <C-g>u breaks the current undo sequence.
    inoremap <silent><expr> <CR> coc#pum#visible() ? coc#pum#confirm()
                                  \: "\<C-g>u\<CR>\<c-r>=coc#on_enter()\<CR>"

    function! CheckBackspace() abort
      let col = col('.') - 1
      return !col || getline('.')[col - 1]  =~# '\s'
    endfunction

    " Use <c-space> to trigger completion
    if has('nvim')
      inoremap <silent><expr> <c-space> coc#refresh()
    else
      inoremap <silent><expr> <c-@> coc#refresh()
    endif
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    local keyset = vim.keymap.set

    function _G.check_back_space()
      local col = vim.fn.col('.') - 1
      return col == 0 or vim.fn.getline('.'):sub(col, col):match('%s') ~= nil
    end

    -- Use Tab for trigger completion with characters ahead and navigate the menu.
    -- NOTE: There's always a completion item selected by default. You may want to
    -- enable no-select by setting "suggest.noselect": true in your config file.
    -- NOTE: Use ':verbose imap <tab>' to make sure Tab is not mapped by another
    -- plugin before adding this to your config.
    local opts = { silent = true, noremap = true, expr = true, replace_keycodes = false }
    keyset('i', '<TAB>', 'coc#pum#visible() ? coc#pum#next(1) : v:lua.check_back_space() ? "<TAB>" : coc#refresh()', opts)
    keyset('i', '<S-TAB>', [[coc#pum#visible() ? coc#pum#prev(1) : "\<C-h>"]], opts)

    -- Make <CR> accept the selected completion item or notify coc.nvim to format.
    -- <C-g>u breaks the current undo sequence.
    keyset('i', '<cr>', [[coc#pum#visible() ? coc#pum#confirm() : "\<C-g>u\<CR>\<c-r>=coc#on_enter()\<CR>"]], opts)

    -- Use <c-j> to trigger snippets
    keyset('i', '<c-j>', '<Plug>(coc-snippets-expand-jump)')

    -- Use <c-space> to trigger completion
    keyset('i', '<c-space>', 'coc#refresh()', { silent = true, expr = true })
    ```
  </Tab>
</Tabs>

<Note>
  Use `:verbose imap <tab>` to confirm your Tab mapping is active and not
  overridden by another plugin.
</Note>

---

## Diagnostics navigation

Jump between diagnostics in the current buffer. Use `:CocDiagnostics` to view all diagnostics in the location list.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Use `[g` and `]g` to navigate diagnostics
    " Use `:CocDiagnostics` to get all diagnostics of current buffer in location list
    nmap <silent> [g <Plug>(coc-diagnostic-prev)
    nmap <silent> ]g <Plug>(coc-diagnostic-next)
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Use `[g` and `]g` to navigate diagnostics
    -- Use `:CocDiagnostics` to get all diagnostics of current buffer in location list
    keyset('n', '[g', '<Plug>(coc-diagnostic-prev)', { silent = true })
    keyset('n', ']g', '<Plug>(coc-diagnostic-next)', { silent = true })
    ```
  </Tab>
</Tabs>

---

## Go to code

Jump to definition, type definition, implementation, and references.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " GoTo code navigation
    nmap <silent> gd <Plug>(coc-definition)
    nmap <silent> gy <Plug>(coc-type-definition)
    nmap <silent> gi <Plug>(coc-implementation)
    nmap <silent> gr <Plug>(coc-references)
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- GoTo code navigation
    keyset('n', 'gd', '<Plug>(coc-definition)', { silent = true })
    keyset('n', 'gy', '<Plug>(coc-type-definition)', { silent = true })
    keyset('n', 'gi', '<Plug>(coc-implementation)', { silent = true })
    keyset('n', 'gr', '<Plug>(coc-references)', { silent = true })
    ```
  </Tab>
</Tabs>

| Key | Action |
| --- | ------ |
| `gd` | Go to definition |
| `gy` | Go to type definition |
| `gi` | Go to implementation |
| `gr` | Go to references |

---

## Hover documentation

Press `K` to show documentation for the symbol under the cursor in a preview window. For Vim help files and Vim filetypes, the built-in `:help` command is used instead.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Use K to show documentation in preview window
    nnoremap <silent> K :call ShowDocumentation()<CR>

    function! ShowDocumentation()
      if CocAction('hasProvider', 'hover')
        call CocActionAsync('doHover')
      else
        call feedkeys('K', 'in')
      endif
    endfunction
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Use K to show documentation in preview window
    function _G.show_docs()
      local cw = vim.fn.expand('<cword>')
      if vim.fn.index({ 'vim', 'help' }, vim.bo.filetype) >= 0 then
        vim.api.nvim_command('h ' .. cw)
      elseif vim.api.nvim_eval('coc#rpc#ready()') then
        vim.fn.CocActionAsync('doHover')
      else
        vim.api.nvim_command('!' .. vim.o.keywordprg .. ' ' .. cw)
      end
    end

    keyset('n', 'K', '<CMD>lua _G.show_docs()<CR>', { silent = true })
    ```
  </Tab>
</Tabs>

---

## Symbol highlighting

Highlight all references to the symbol under the cursor when the cursor is idle. This uses the `CursorHold` autocmd, which fires after `updatetime` milliseconds.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Highlight the symbol and its references when holding the cursor
    autocmd CursorHold * silent call CocActionAsync('highlight')
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Highlight the symbol and its references on a CursorHold event (cursor is idle)
    vim.api.nvim_create_augroup('CocGroup', {})
    vim.api.nvim_create_autocmd('CursorHold', {
      group = 'CocGroup',
      command = "silent call CocActionAsync('highlight')",
      desc = 'Highlight symbol under cursor on CursorHold'
    })
    ```
  </Tab>
</Tabs>

---

## Refactoring

Rename symbols, format selected code, apply code actions, and run refactor actions.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Symbol renaming
    nmap <leader>rn <Plug>(coc-rename)

    " Formatting selected code
    xmap <leader>f  <Plug>(coc-format-selected)
    nmap <leader>f  <Plug>(coc-format-selected)

    augroup mygroup
      autocmd!
      " Setup formatexpr for specified filetypes
      autocmd FileType typescript,json setl formatexpr=CocAction('formatSelected')
    augroup end

    " Applying code actions to the selected code block
    " Example: `<leader>aap` for current paragraph
    xmap <leader>a  <Plug>(coc-codeaction-selected)
    nmap <leader>a  <Plug>(coc-codeaction-selected)

    " Remap keys for applying code actions at the cursor position
    nmap <leader>ac  <Plug>(coc-codeaction-cursor)
    " Remap keys for applying code actions affecting the whole buffer
    nmap <leader>as  <Plug>(coc-codeaction-source)
    " Apply the most preferred quickfix action to fix diagnostic on the current line
    nmap <leader>qf  <Plug>(coc-fix-current)

    " Remap keys for applying refactor code actions
    nmap <silent> <leader>re <Plug>(coc-codeaction-refactor)
    xmap <silent> <leader>r  <Plug>(coc-codeaction-refactor-selected)
    nmap <silent> <leader>r  <Plug>(coc-codeaction-refactor-selected)
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Symbol renaming
    keyset('n', '<leader>rn', '<Plug>(coc-rename)', { silent = true })

    -- Formatting selected code
    keyset('x', '<leader>f', '<Plug>(coc-format-selected)', { silent = true })
    keyset('n', '<leader>f', '<Plug>(coc-format-selected)', { silent = true })

    -- Setup formatexpr for specified filetypes
    vim.api.nvim_create_autocmd('FileType', {
      group = 'CocGroup',
      pattern = 'typescript,json',
      command = "setl formatexpr=CocAction('formatSelected')",
      desc = 'Setup formatexpr specified filetype(s).'
    })

    -- Apply codeAction to the selected region.
    -- Example: `<leader>aap` for current paragraph
    local opts = { silent = true, nowait = true }
    keyset('x', '<leader>a', '<Plug>(coc-codeaction-selected)', opts)
    keyset('n', '<leader>a', '<Plug>(coc-codeaction-selected)', opts)

    -- Remap keys for applying code actions at the cursor position.
    keyset('n', '<leader>ac', '<Plug>(coc-codeaction-cursor)', opts)
    -- Remap keys for applying source code actions for the current file.
    keyset('n', '<leader>as', '<Plug>(coc-codeaction-source)', opts)
    -- Apply the most preferred quickfix action on the current line.
    keyset('n', '<leader>qf', '<Plug>(coc-fix-current)', opts)

    -- Remap keys for applying refactor code actions.
    keyset('n', '<leader>re', '<Plug>(coc-codeaction-refactor)', { silent = true })
    keyset('x', '<leader>r', '<Plug>(coc-codeaction-refactor-selected)', { silent = true })
    keyset('n', '<leader>r', '<Plug>(coc-codeaction-refactor-selected)', { silent = true })
    ```
  </Tab>
</Tabs>

| Key | Mode | Action |
| --- | ---- | ------ |
| `<leader>rn` | Normal | Rename symbol |
| `<leader>f` | Normal, Visual | Format selected code |
| `<leader>a` | Normal, Visual | Code action on selection |
| `<leader>ac` | Normal | Code action at cursor |
| `<leader>as` | Normal | Code action for whole buffer (source) |
| `<leader>qf` | Normal | Apply preferred quickfix on current line |
| `<leader>re` | Normal | Refactor action at cursor |
| `<leader>r` | Normal, Visual | Refactor action on selection |

---

## Code Lens

Run the Code Lens action on the current line.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Run the Code Lens action on the current line
    nmap <leader>cl  <Plug>(coc-codelens-action)
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Run the Code Lens actions on the current line
    keyset('n', '<leader>cl', '<Plug>(coc-codelens-action)', opts)
    ```
  </Tab>
</Tabs>

---

## Text objects

Map function and class text objects to operate on entire function or class bodies. Requires `textDocument.documentSymbol` support from the language server.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Map function and class text objects
    " NOTE: Requires 'textDocument.documentSymbol' support from the language server
    xmap if <Plug>(coc-funcobj-i)
    omap if <Plug>(coc-funcobj-i)
    xmap af <Plug>(coc-funcobj-a)
    omap af <Plug>(coc-funcobj-a)
    xmap ic <Plug>(coc-classobj-i)
    omap ic <Plug>(coc-classobj-i)
    xmap ac <Plug>(coc-classobj-a)
    omap ac <Plug>(coc-classobj-a)
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Map function and class text objects
    -- NOTE: Requires 'textDocument.documentSymbol' support from the language server
    local opts = { silent = true, nowait = true }
    keyset('x', 'if', '<Plug>(coc-funcobj-i)', opts)
    keyset('o', 'if', '<Plug>(coc-funcobj-i)', opts)
    keyset('x', 'af', '<Plug>(coc-funcobj-a)', opts)
    keyset('o', 'af', '<Plug>(coc-funcobj-a)', opts)
    keyset('x', 'ic', '<Plug>(coc-classobj-i)', opts)
    keyset('o', 'ic', '<Plug>(coc-classobj-i)', opts)
    keyset('x', 'ac', '<Plug>(coc-classobj-a)', opts)
    keyset('o', 'ac', '<Plug>(coc-classobj-a)', opts)
    ```
  </Tab>
</Tabs>

| Key | Description |
| --- | ----------- |
| `if` | Inner function |
| `af` | Around function |
| `ic` | Inner class |
| `ac` | Around class |

These work as motions: `vif` selects the inner function body, `daf` deletes the entire function including its signature.

---

## Scroll float windows

Remap `<C-f>` and `<C-b>` to scroll coc.nvim floating windows or popups when they are visible. When no float is open, the keys fall back to normal page scrolling.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Remap <C-f> and <C-b> to scroll float windows/popups
    if has('nvim-0.4.0') || has('patch-8.2.0750')
      nnoremap <silent><nowait><expr> <C-f> coc#float#has_scroll() ? coc#float#scroll(1) : "\<C-f>"
      nnoremap <silent><nowait><expr> <C-b> coc#float#has_scroll() ? coc#float#scroll(0) : "\<C-b>"
      inoremap <silent><nowait><expr> <C-f> coc#float#has_scroll() ? "\<c-r>=coc#float#scroll(1)\<cr>" : "\<Right>"
      inoremap <silent><nowait><expr> <C-b> coc#float#has_scroll() ? "\<c-r>=coc#float#scroll(0)\<cr>" : "\<Left>"
      vnoremap <silent><nowait><expr> <C-f> coc#float#has_scroll() ? coc#float#scroll(1) : "\<C-f>"
      vnoremap <silent><nowait><expr> <C-b> coc#float#has_scroll() ? coc#float#scroll(0) : "\<C-b>"
    endif
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Remap <C-f> and <C-b> to scroll float windows/popups
    local opts = { silent = true, nowait = true, expr = true }
    keyset('n', '<C-f>', 'coc#float#has_scroll() ? coc#float#scroll(1) : "<C-f>"', opts)
    keyset('n', '<C-b>', 'coc#float#has_scroll() ? coc#float#scroll(0) : "<C-b>"', opts)
    keyset('i', '<C-f>',
      'coc#float#has_scroll() ? "<c-r>=coc#float#scroll(1)<cr>" : "<Right>"', opts)
    keyset('i', '<C-b>',
      'coc#float#has_scroll() ? "<c-r>=coc#float#scroll(0)<cr>" : "<Left>"', opts)
    keyset('v', '<C-f>', 'coc#float#has_scroll() ? coc#float#scroll(1) : "<C-f>"', opts)
    keyset('v', '<C-b>', 'coc#float#has_scroll() ? coc#float#scroll(0) : "<C-b>"', opts)
    ```
  </Tab>
</Tabs>

---

## Selection range

Expand or shrink the visual selection by semantic range. Requires `textDocument/selectionRange` support from the language server.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Use CTRL-S for selections ranges
    " Requires 'textDocument/selectionRange' support of language server
    nmap <silent> <C-s> <Plug>(coc-range-select)
    xmap <silent> <C-s> <Plug>(coc-range-select)
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Use CTRL-S for selections ranges
    -- Requires 'textDocument/selectionRange' support of language server
    keyset('n', '<C-s>', '<Plug>(coc-range-select)', { silent = true })
    keyset('x', '<C-s>', '<Plug>(coc-range-select)', { silent = true })
    ```
  </Tab>
</Tabs>

---

## Commands

Add convenient ex-commands for common coc.nvim actions.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Add `:Format` command to format current buffer
    command! -nargs=0 Format :call CocActionAsync('format')

    " Add `:Fold` command to fold current buffer
    command! -nargs=? Fold :call CocAction('fold', <f-args>)

    " Add `:OR` command for organize imports of the current buffer
    command! -nargs=0 OR   :call CocActionAsync('runCommand', 'editor.action.organizeImport')
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Add `:Format` command to format current buffer
    vim.api.nvim_create_user_command('Format', "call CocAction('format')", {})

    -- Add `:Fold` command to fold current buffer
    vim.api.nvim_create_user_command('Fold', "call CocAction('fold', <f-args>)", { nargs = '?' })

    -- Add `:OR` command for organize imports of the current buffer
    vim.api.nvim_create_user_command('OR', "call CocActionAsync('runCommand', 'editor.action.organizeImport')", {})
    ```
  </Tab>
</Tabs>

| Command | Description |
| ------- | ----------- |
| `:Format` | Format the current buffer |
| `:Fold` | Fold the current buffer (optional fold kind argument) |
| `:OR` | Organize imports in the current buffer |

---

## Statusline

Prepend coc.nvim status and the current function name to the native Vim statusline.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Add (Neo)Vim's native statusline support
    " NOTE: Please see `:h coc-status` for integrations with external plugins that
    " provide custom statusline: lightline.vim, vim-airline
    set statusline^=%{coc#status()}%{get(b:,'coc_current_function','')}
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Add (Neo)Vim's native statusline support
    -- NOTE: Please see `:h coc-status` for integrations with external plugins that
    -- provide custom statusline: lightline.vim, vim-airline
    vim.opt.statusline:prepend("%{coc#status()}%{get(b:,'coc_current_function','')}")
    ```
  </Tab>
</Tabs>

See the [Statusline Integration](/guides/statusline) guide for lightline, airline, and lualine setup.

---

## CocList mappings

Open CocList views for diagnostics, extensions, commands, symbols, and list navigation.

<Tabs>
  <Tab title="Vimscript">
    ```vim
    " Mappings for CoCList
    " Show all diagnostics
    nnoremap <silent><nowait> <space>a  :<C-u>CocList diagnostics<cr>
    " Manage extensions
    nnoremap <silent><nowait> <space>e  :<C-u>CocList extensions<cr>
    " Show commands
    nnoremap <silent><nowait> <space>c  :<C-u>CocList commands<cr>
    " Find symbol of current document
    nnoremap <silent><nowait> <space>o  :<C-u>CocList outline<cr>
    " Search workspace symbols
    nnoremap <silent><nowait> <space>s  :<C-u>CocList -I symbols<cr>
    " Do default action for next item
    nnoremap <silent><nowait> <space>j  :<C-u>CocNext<CR>
    " Do default action for previous item
    nnoremap <silent><nowait> <space>k  :<C-u>CocPrev<CR>
    " Resume latest coc list
    nnoremap <silent><nowait> <space>p  :<C-u>CocListResume<CR>
    ```
  </Tab>
  <Tab title="Lua">
    ```lua
    -- Mappings for CoCList
    local opts = { silent = true, nowait = true }
    -- Show all diagnostics
    keyset('n', '<space>a', ':<C-u>CocList diagnostics<cr>', opts)
    -- Manage extensions
    keyset('n', '<space>e', ':<C-u>CocList extensions<cr>', opts)
    -- Show commands
    keyset('n', '<space>c', ':<C-u>CocList commands<cr>', opts)
    -- Find symbol of current document
    keyset('n', '<space>o', ':<C-u>CocList outline<cr>', opts)
    -- Search workspace symbols
    keyset('n', '<space>s', ':<C-u>CocList -I symbols<cr>', opts)
    -- Do default action for next item
    keyset('n', '<space>j', ':<C-u>CocNext<cr>', opts)
    -- Do default action for previous item
    keyset('n', '<space>k', ':<C-u>CocPrev<cr>', opts)
    -- Resume latest coc list
    keyset('n', '<space>p', ':<C-u>CocListResume<cr>', opts)
    ```
  </Tab>
</Tabs>

| Key | Description |
| --- | ----------- |
| `<space>a` | Show all diagnostics |
| `<space>e` | Manage extensions |
| `<space>c` | Show available commands |
| `<space>o` | Document outline (symbols) |
| `<space>s` | Search workspace symbols |
| `<space>j` | Default action for next CocList item |
| `<space>k` | Default action for previous CocList item |
| `<space>p` | Resume the latest CocList |
